export const CATEGORIAS: string[] = [];

export function mapear(c: string): string {
  return c;
}

export function categoriaToSubcategoria(_codigo: string | null): string | null {
  return null;
}

export function grupoFromCategoria(
  _codigo: string | null,
  grupo: string | null,
): string | null {
  return grupo;
}

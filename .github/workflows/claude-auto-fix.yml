name: Claude Auto Fix (DESACTIVADO)
# Workflow desactivado para no consumir API key de Anthropic.
# Trabajo manual vía MCP desde chat Claude.ai.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pipeline desactivado intencionadamente. No consume API."
